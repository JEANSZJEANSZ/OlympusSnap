{
  "name": "olympus-snap-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply olympus-snap --local",
    "db:migrate:remote": "wrangler d1 migrations apply olympus-snap --remote"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
