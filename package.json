{
  "name": "olympus-snap",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "npm run dev --prefix worker",
    "worker:deploy": "npm run deploy --prefix worker",
    "worker:db:local": "npm run db:migrate:local --prefix worker",
    "worker:db:remote": "npm run db:migrate:remote --prefix worker"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.4",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@fontsource/press-start-2p": "^5.3.0",
    "animejs": "^4.5.0",
    "konva": "^10.3.0",
    "matter-js": "^0.20.0",
    "qrcode": "^1.5.4",
    "three": "^0.185.1"
  }
}
