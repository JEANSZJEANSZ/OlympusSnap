# Copy to .env (never commit .env).
# Leave empty so the SPA talks to same-origin /api (Vite proxy → wrangler).
# VITE_API_BASE=http://127.0.0.1:8787

# Optional. QR origin when guests scan a different host than the tablet.
# VITE_PUBLIC_ORIGIN=https://example.com
