# Optional — not required.
# QR origin falls back to location.origin when VITE_PUBLIC_ORIGIN is unset.
# Set this when the booth URL guests scan must differ from the tablet’s origin.
# VITE_PUBLIC_ORIGIN=https://example.com
