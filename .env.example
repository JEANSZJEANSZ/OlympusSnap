# Copy to .env (never commit .env).
# Admin unlock PIN — required for production / vite preview.
# npm run dev still skips the Cerberus gate.
VITE_ADMIN_PIN=

# Optional. QR origin falls back to location.origin when unset.
# VITE_PUBLIC_ORIGIN=https://example.com
